server:
  port: 80
zuul:
  ignored-services: '*'
  routes:
    auth-service:
      path: /uaa/**
      stripPrefix: true
      sensitiveHeaders:
    user-service:
      path: /user/**
      sensitiveHeaders:
    article-service:
      path: /article/**
      sensitiveHeaders:
