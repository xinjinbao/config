server:
  port: 80
zuul:
  ignored-services: '*'
  routes:
    auth-server:
      path: /uaa/**
      stripPrefix: true
      sensitiveHeaders:
    user-service:
      path: /user/**
      sensitiveHeaders:
    article-service:
      path: /article/**
      sensitiveHeaders:
#    monitor-server:
#      path: /monitor/**
#    tracker-server:
#      path: /tracker/**
